<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mega.s1.movie.movieFile.MovieFileRepository">

	<select id="movieFileSelect" parameterType="MovieVO" resultType="MovieFileVO">
		select * from movieFile where movieNum=#{movieNum} and status=2
	</select>
	<select id="videoFileSelect" parameterType="MovieVO" resultType="MovieFileVO">
		select * from movieFile where movieNum=#{movieNum} and status=1
	</select>
	
	<select id="imageCount" parameterType="MovieVO" resultType="Long">
		select count(fileNum) from movieFile where movieNum=#{movieNum} and status=2
	</select>
	<select id="videoCount" parameterType="MovieVO" resultType="Long">
		select count(fileNum) from movieFile where movieNum=#{movieNum} and status=1
	</select>

</mapper>