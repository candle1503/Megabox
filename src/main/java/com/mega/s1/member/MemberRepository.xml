<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mega.s1.member.MemberRepository">
	<insert id="setJoin" parameterType="MemberVO">
		insert into member values (#{id},#{password},#{name},#{phone},#{email},#{age},now(),1000)
	</insert>
	
	<select id="setLogin" parameterType="MemberVO" resultType="MemberVO">
		select * from member where id=#{id} and password=#{password}	
	</select>
	
	<select id="idCheck" parameterType="MemberVO" resultType="MemberVO">
		select * from member where id=#{id}	
	</select>
	
	<delete id="setDelete" parameterType="MemberVO">
		delete from member where id=#{id}	
	</delete>
	
	<update id="setUpdate" parameterType="MemberVO">
		update member set password=#{password},email=#{email}, phone= #{phone} where id=#{id}
	</update>
	
	<select id="getTicketCount" parameterType="MemberVO" resultType="Integer">
		select count(ticketNum) from ticket where id=#{id}
	</select>
	
	<select id="getReviewCount" parameterType="MemberVO" resultType="Integer">
		select count(reviewNum) from review where writer=#{id}
	</select>
	
	<select id="getReviewList" parameterType="MemberVO" resultType="ReviewVO">
		select * from review where writer=#{id} order by reviewNum desc
	</select>
	
	<select id="getMovieImage" parameterType="Integer" resultType="String">
		select fileName from movieFile where movieNum=#{movieNum} and status=2 limit 1
	</select>
	
	<select id="getMovieName" parameterType="Integer" resultType="String">
		select name from movie where movieNum=#{movieNum}
	</select>
	
	<delete id="deleteReview" parameterType="ReviewVO">
		delete from review where reviewNum=#{reviewNum}
	</delete>
	
	<update id="updateTicket" parameterType="String">
		update ticket set status = 1 where status=0 and now() > viewDate 
	</update>
	
	<select id="bookedCompleteList" parameterType="String" resultType="TicketVO">
		select * from ticket where status = 0 and id = #{id}
	</select>
	
	<select id="bookedCompleteAfterList" parameterType="String" resultType="TicketVO">
		select * from ticket where status = 1 and id = #{id}
	</select>
	
	<update id="deleteReviewStatus" parameterType="TicketVO">
		update ticket set status = 1 where status=2 and id=#{id} 
	</update>
	
	<select id="bookedCompleteListCalandar" parameterType="TicketVO" resultType="TicketVO">
		select * from ticket where status = 1 and id = #{id} and str_to_date(#{calandarDate2} , '%Y-%m-%d') > viewDate and  viewDate >= str_to_date(#{calandarDate} , '%Y-%m-%d')	
	</select>
	
	<select id="getMemberInfo" parameterType="MemberVO" resultType="MemberVO">
		select * from member where id=#{id}
	</select>
	
</mapper>