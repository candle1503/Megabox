<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.mega.s1.booking.BookingRepository">

	<select id="bookingMovieList" parameterType="BookingVO" resultType="BookingVO">
		select R.*, M.*
		from roomMovieTime R left join movie M
		on (R.movieNum = M.movieNum)
		where date_format(startTime, '%d') = #{startTime}
	</select>
	

</mapper>